; Code outline for Lichen.  Lichen has no explicit function/type declarations;
; functions and values are `name = ...` bindings (optionally `let`-prefixed), so
; every binding becomes an outline entry labelled by its name.
(binding
  name: (identifier) @name) @item
