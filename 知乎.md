# LichenVM

*一个惰性求值、基于合一(unification)的类型检查器基础设施——在这里,类型就是一种值。*

## Highlevel(高层)

从 IR(中间表示)出发构建程序,检查(checking)与运行时(runtime)是统一的。

### 特性

- Hindley–Milner 类型系统
  - 处处 Let 多态(Let-Polymorphism Everywhere)
- 依赖类型:通过惰性求值实现。
- 一等类型:通过偏函数应用实现类型实例化。

### 理念

- 无柯里化(No Curry)
- `Type : Type`(类型本身也是类型)

## Lowlevel(底层)

一个类型与值不区分的解释器。

### 特性

- 惰性求值(Lazy Evaluation)。
- 块级垃圾回收(Block Level Garbage Collection)。
- λ 演算:一等函数、高阶函数、闭包。
- 递归函数应用。
- 函数规范化(Function Normalization)。
- 合一(Unification)。
- 基于宏的枚举扩展(WIP):可以在不影响性能的前提下实现你自己的类型检查器。

### 理念

- 最小内存占用。
- 最少分配。
- 简单程序的高速运行(复杂程序未来将交给 JIT 编译)。

## Language(示例语言)

一个演示上述特性的最小语言。

- [规范](docs/language-spec.md)

## 快速开始

需要 Rust 工具链。

```bash
cargo build
cargo test
```

运行全部示例程序(每个一行 `文件: 输出`):

```bash
cargo run -p lichen-language -- crates/lichen-language/examples/programs
```

运行单个程序:

```bash
cargo run -p lichen-language -- crates/lichen-language/examples/programs/bindings.lichen
# 1
```

安装 CLI(名为 `lichen` 的可执行文件):

```bash
cargo install --path crates/lichen-language  # 在本仓库的检出目录中执行
cargo install --git git@github.com:windwhiterain/lichen-vm.git lichen-language
```

然后直接运行:

```bash
lichen crates/lichen-language/examples/programs/bindings.lichen
# 1
```

## 示例

本节由 `cargo run -p lichen-language --bin sync-readme` 从 [`examples/programs`](crates/lichen-language/examples/programs) 自动生成,并由 [`tests/readme.rs`](crates/lichen-language/tests/readme.rs) 保证同步。

<!-- begin: examples -->

### `array.lichen`(数组)

```text
[1, 2, 3] : Int<3>
```

输出:
```text
[1, 2, 3]: Int<3>
```

### `tuple.lichen`(元组)

```text
(1, Int) : <Int, Type>
```

输出:
```text
[1, Int]: <Int, Type>
```

### `index.lichen`(索引)

```text
a = [1, 2]
b = (1, Int)
(a[0], a[1], b[0], b[1])
```

输出:
```text
[1, 2, 1, Int]: <Int, Int, Int, Type>
```

### `closure.lichen`(闭包)

```text
a = 1
f1 = x => {
    b = 2
    f2 = y => [a, b, x, y]
    f2
}
f1 3 4 
```

输出:
```text
[1, 2, 3, 4]: Int<4>
```

### `dependent_type.lichen`(依赖类型)

```text
a = x => (1, Int)[x]
(a 0, a 1)
```

输出:
```text
[1, Int]: <Int, Type>
```

### `let_polymorphism.lichen`(let 多态)

```text
f = x => x
(f 1, f Int)
```

输出:
```text
[1, Int]: <Int, Type>
```

### `nested_function.lichen`(嵌套函数)

```text
f1 = x => {
    f2 = y => [y, y]
    f2 x 
}
f1 1
```

输出:
```text
[1, 1]: ?a
```

### `placeholder.lichen`(占位符)

```text
f1 = x => x : Int
f2 = x => {
    x: <Type, _>
    f1 x[1]
}
f2
```

输出:
```text
Function: <Type, [?a, ?b]> -> ?c
```

### `structs.lichen`(结构体)

```text
s = struct<Int, Type>
a = s(1, Int)
(a, a[0], a[1])
```

输出:
```text
[[1, Int], 1, Int]: <struct<Int, Type>, Int, Type>
```

<!-- end: examples -->
